<Page x:Class="HEADMEN_EYE.View.PassesList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:HEADMEN_EYE.View"
      xmlns:vm="clr-namespace:HEADMEN_EYE.ViewModels"
      mc:Ignorable="d" 
      Height="510" Width="710"
      Title="PassesList">

    <Page.DataContext>
        <vm:PassesListPageViewModel></vm:PassesListPageViewModel>
    </Page.DataContext>
    <ListView HorizontalAlignment="Left" VerticalAlignment="Top" Height="490" Width="680" Margin="10,10,0,0" ItemsSource="{Binding Dataview}" FontSize="16.5" Foreground="Black" BorderBrush="#FFCCD2F9">
        <ListView.View>
            <GridView>
                <GridViewColumn DisplayMemberBinding="{Binding Path=NameStdnt}" Header="Name" Width="180"/>
                <GridViewColumn DisplayMemberBinding="{Binding Path=SurnameStdnt}" Header="Surname" Width="180"/>
                <GridViewColumn DisplayMemberBinding="{Binding Path=StudentGroup}" Header="Group" Width="180"/>
                <GridViewColumn DisplayMemberBinding="{Binding Path=Passes}" Header="Passes" Width="110"/>
            </GridView>
        </ListView.View>
    </ListView>
</Page>
