<Page x:Class="HEADMEN_EYE.View.GroupList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:HEADMEN_EYE.View"
      xmlns:vm="clr-namespace:HEADMEN_EYE.ViewModels"
      mc:Ignorable="d" 
      Height="510" Width="710"
      Title="GroupList" Background="#FFCCD2F9">
    <Page.DataContext>
        <vm:GroupListPageViewModel></vm:GroupListPageViewModel>
    </Page.DataContext>
    <ListView HorizontalAlignment="Left" VerticalAlignment="Top" Height="490" Width="690" Margin="10,10,0,0" ItemsSource="{Binding dataView}">
        <ListView.View>
            <GridView>
                <GridViewColumn DisplayMemberBinding="{Binding Path=NameStdnt}" Header="Name" Width="170"/>
                <GridViewColumn DisplayMemberBinding="{Binding Path=SurnameStdnt}" Header="Surname" Width="170"/>
                <GridViewColumn DisplayMemberBinding="{Binding Path=PatronymicStdnt}" Header="Patronymic" Width="170"/>
                <GridViewColumn DisplayMemberBinding="{Binding Path=StudentGroup}" Header="Group" Width="170"/>
            </GridView>
        </ListView.View>
    </ListView>
</Page>
